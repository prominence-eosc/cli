#!/usr/bin/env python
import sys
from prominence.cli import create_parser, main

if __name__ == "__main__":
    if len(sys.argv) < 2:
        create_parser().print_help(sys.stderr)
        exit(0)

    main()
